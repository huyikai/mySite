---
import BaseLayout from '@layouts/BaseLayout.astro';
import ProjectsView from '@components/views/ProjectsView.astro';
import { default as ui } from '../../i18n/ui';
const t = ui.en;
---
<BaseLayout locale="en" pathKey="/projects/" pageTitle={`${t.projects.title} · ${t.siteName}`}>
  <ProjectsView locale="en" />
</BaseLayout>
